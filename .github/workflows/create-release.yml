name: Automatically bump patch version

on:
  push:
    branches:
      - master

jobs:
  Autobump:
    uses: DanySK/workflows/.github/workflows/compute-version-and-create-release.yml@1.0.497
    with:
      update-version: patch
