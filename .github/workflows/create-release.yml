name: Automatically bump patch version

on:
  push:
    branches:
      - master

jobs:
  Autobump:
    uses: DanySK/workflows/.github/workflows/compute-version-and-create-release.yml@1.0.201
    with:
      update-version: patch
